# Python dependencies for usdpython tools
# Install using: python3 -m pip install -r requirements.txt
# Recommended to use a virtual environment.

usd-core>=24.0.0  # USD Python bindings (works with Python 3.9+)
numpy>=1.18.0  # For glTF processing
Pillow>=8.0.0  # For iOS 12 texture conversion
